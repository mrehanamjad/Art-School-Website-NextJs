/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com','https://cdn.pixabay.com','cdn.pixabay.com','assets.aceternity.com']
    }
};

export default nextConfig;
